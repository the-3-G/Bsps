import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // No `output: "export"` — Vercel handles SSR/SSG natively
  // No `images.unoptimized` — Vercel provides built-in image optimization
};

export default nextConfig;
